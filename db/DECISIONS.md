# DECISIONS.md — 결정 이력

**append-only.** 지우지 않는다. 뒤집힌 결정은 지우는 대신 새 행을 추가하고
맨 아래 표에 원래 결정을 가리킨다.

> **규칙: 코드 커밋에 새 결정이나 뒤집힌 결정이 들어 있으면 같은 커밋에서
> 이 파일을 고친다.** `migrations/README.md` 의 "델타를 쓰면 `schema.sql` 도
> 같은 커밋에서" 와 같은 이유다. 실제로 002·003 에서 두 번 밀렸고, 두 번 다
> 뒤늦게 채웠다 — 규칙이 파일 안에 없으면 다음에도 잊는다.

번호는 결정 순서다. 근거의 상세는 `SCHEMA.md`, 수치는 `tuning.yaml`,
수치의 근거는 `TUNING.md`.

---

## 2026-08-26 (저녁) — 5~8단계 주간 배치

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 123 | **5~8단계를 한 워크플로로.** 이슈는 배치 내부 일시 데이터 | 나누면 중간 결과를 넘길 영속 계층이 필요해지고 **안 만들기로 한 `issues` 저장소(#50)를 다시 요구하게 된다.** 사람이 5단계 결과를 검토하고 6단계를 실행하는 경계도 없다. 재실행 단위도 5~8단계 전체다 | 5~8단계 |
| 124 | **`running` 배치를 이어받는다. 자동 만료(TTL) 없음.** 새 배치로 갈아타려면 명시적 `abort` | 재실행이 같은 `as_of`·`window`·`candidate_snapshot` 을 써야 #48·#61 의 재현성이 성립한다. TTL 의 N 에 근거가 없고(2시간이어도 12시간이어도 된다), 자동 만료는 **"n8n 죽음"과 "배치 폐기"를 다시 섞는다** — 프로세스 장애가 배치를 무효로 만들지 않는다. "월요일마다 옛 배치만 이어받는" 상황은 **그 배치가 계속 실패한다는 뜻**이고, 조용히 새 `as_of` 를 만들면 장애가 가려진다 | 5단계 |
| 125 | **`POST /complete` 한 번. 전부 아니면 전무.** 부분 저장하지 않는다 | `/ingest` 와 성격이 다르다 — 거기선 기사 50건이 독립이라 1건이 깨져도 49건이 유효하지만, 여기선 topics·소비 마커·stale·집계가 **합쳐서 하나의 배치 결과**다. 소재 하나만 실패했는데 32개를 저장하면 즉시 답 없는 질문이 생긴다: 실패한 소재의 기사를 소비할까(안 하면 부분 완료 배치), batch 를 done 으로 둘까(완전성이 깨짐) running 으로 둘까(재실행이 이미 저장된 32개를 어떻게 다룰지 또 상태가 생김). **#47 의 장점이 통째로 사라진다** | 5~8단계 |
| 126 | **`n = 1` subject 는 코드가 singleton 을 만든다.** LLM 을 안 부른다 | **근거는 비용이 아니라 층위다.** 기사가 하나뿐이면 판단할 경우의 수가 없다 — 결과는 반드시 singleton 하나다. "셀 수 있는 건 코드가 센다"의 연장. 소스가 늘어 이 경우가 사라져도 **분기가 자연스럽게 안 타게 될 뿐** 설계를 다시 볼 일이 없다. 코드는 **새 의미를 만들지 않는다** — `issue_signal`·`cluster_reason` 을 기사 분석 결과 그대로 쓴다 | 5단계 |
| 127 | **검증 5종 — 형식·소속·중복·누락·subject. 누락을 코드가 보정하지 않는다** | 불변식 하나로 정리된다: `candidate_snapshot = ⋃ topic.article_ids` 이고 각 id 는 정확히 한 번. **`n=1` 코드 생성은 정상 경로지만 `n≥2` 누락 보정은 repair 다** — 자동 보정하면 LLM 이 완전 분할을 지켰는지 검증할 수 없고, 빠진 기사가 다른 기사와 묶여야 했던 건지 singleton 이어야 했던 건지 코드는 알 수 없다. `issue_signal` 검증 실패를 repair 하지 않기로 한 것(#25)과 같은 계열 | 5~8단계 |
| 128 | **`batches.candidate_snapshot` 신설**(마이그레이션 004, user_version 4). 후보를 고정한다 | #127 의 계약이 생기면서 후보 집합이 **조회 결과가 아니라 검증 기준값**이 됐다. 기준값을 complete 시점에 재계산하면 기준 자체가 움직인다 — 그 사이 사람이 기사를 `excluded` 로 내리면 62→61 이 되고 "누락 1건"으로 422 가 나는데 **원인이 LLM 이 아니라 사람인데 안 보인다.** `subject_id` 도 담는다: 기사 집합만 고정하면 subject 검증만 현재 DB 값을 보게 되어 그 지점만 흔들린다(#51·#121 의 연장) | 5단계 |
| 129 | **6단계 User Prompt 에서 기사 수·매체 수를 뺀다** | System Prompt 가 정확히 그 둘을 점수에 반영하지 말라고 하는데 **라벨까지 붙여 입력으로 줬다.** #37 이 이 점수를 순수 서비스 직접성 축으로 좁힌 이유가 "모델이 추정하면 축이 침범된다"였다. `article_ids` 길이로 유추되긴 하지만 그건 구조적으로 필요한 입력에서 부수적으로 드러나는 것이고, **평가 축처럼 제시하는 것과는 다르다.** #37 을 더 엄격하게 구현하는 수정이다 | 6단계 |
| 130 | **`db.py` 에 `isolation_level=None`** | 기본값(`''`)이면 파이썬 `sqlite3` 가 암묵 트랜잭션을 자동으로 열고 닫는다. **우리 `BEGIN IMMEDIATE` 가 무시되고 `COMMIT` 이 엉뚱하게 동작했다** — `/batches/start` 의 INSERT 가 커밋되지 않고 사라지는 걸로 드러났다. `/ingest`·`/classify`·`/analyze` 도 같은 상태였다: 결과는 맞았지만 **파이썬의 암묵 커밋에 의존해 우연히 동작한 것**이다. 특히 #98 의 "동시 요청 10개 → 200 하나 + 409 아홉"을 `BEGIN IMMEDIATE` 덕분이라고 결론냈는데 그때는 걸려 있지도 않았다 | 인프라 |
| 131 | **`tools/export_workflows.py`.** n8n DB 를 읽어 `n8n/*.json` 으로 뽑는다 | 화면에서 Download → FileZilla 업로드 왕복을 없앤다. 읽기 전용(`mode=ro`). **런타임 값을 뺀다** — `versionId`·`updatedAt`·`triggerCount`·`pinData`·`staticData`·`meta`. 안 빼면 실행할 때마다 diff 가 생겨 무엇이 실제로 바뀌었는지 못 본다. **n8n 데이터 폴더를 레포 안으로 옮기지는 않는다**: `database.sqlite` 는 16M 바이너리고 **암호화된 크레덴셜이 들어 있다** — `esg.db` 를 커밋 안 하는 것과 같은 이유로 런타임 상태와 레포를 섞지 않는다 | 인프라 |

### n8n 에서 또 배운 것

| | |
| --- | --- |
| AI 노드 출력 | **입력 필드가 없다.** 에이전트가 아이템을 통째로 갈아치운다. 이슈 검증이 `subject_id` 로 렌더링 원본을 찾게 짰더니 `find` 가 항상 `undefined` 였고, `expected` 가 빈 집합이 되어 **12개 subject 전부 `partition_broken`** 이 났다. LLM 은 제대로 묶었는데 검증이 못 알아본 것이다 |
| 인덱스 매칭 | 그래서 인덱스로 맞추는데, **코드 갈래를 먼저 합치면 순서가 섞여 인덱스도 못 쓴다.** 검증을 AI 갈래에서 먼저 하고 그다음 합친다 |
| If 노드 boolean | `singleValue: true` 가 필요하고 `typeValidation` 은 `loose` 여야 한다. 정상 아이템에 `__skip` 이 아예 없어(`undefined`) `strict` 면 "string 인데 boolean 을 기대했다"로 막힌다 |

### 검증한 것 (2026-08-26 저녁)

| 항목 | 결과 |
| --- | --- |
| `/complete` 검증 7종 | `missing_article` 62(빈 topics) · `article_not_in_candidates` · `business_relevance_out_of_range` · `bad_aid_format` · `duplicate_article` · `subject_mismatch` · 정상 통과. **여러 문제를 한 번에 보고한다** |
| `running` 이어받기 | 두 번째 `start` 가 `resumed: true` 에 **같은 `as_of`** |
| 스냅샷 고정 | 두 번째 `candidates` 가 `snapshot_created: false` — 재조회하지 않는다 |
| 동시 요청 (#98 재검증) | `isolation_level` 을 고친 뒤 다시 돌려 200 하나 + 409 아홉. **이번엔 근거가 맞는 상태다** |
| 첫 배치 | 62기사 → 51이슈 → 51소재 · 소비 62 + stale 11 · `batch done` |
| 원자성 실전 | 클러스터링 12개가 전부 깨진 첫 시도에서 **DB 쓰기 0** · batch 는 `running` 유지 → 같은 `as_of` 로 재실행 |

### 첫 배치 관찰

- **`n_citable=0` 이 41/51.** 상위 10건 중 8건이 근거 링크가 없다. 임팩트온이
  후보의 81% 인데 `can_cite=0`(페이월)이라서다. **파이프라인을 막지는 않는다** —
  소재는 "이 주제로 쓰자"는 아이디어고 실제 글은 1차 출처를 인용한다(#9 그대로).
  진짜 신호는 **국내 인용 가능 소스가 환경미디어 하나뿐**이라는 것이고,
  그건 크롤 어댑터 작업이다
- **복수 이슈 9개 중 5개가 1매체다.** 외부 리뷰가 "가장 넓다"고 지목한
  묶음(4건 · 전환금융 자금 조성)이 정확히 매체 1개였다 — 여러 매체가 같은
  흐름을 보도한 게 아니라 **한 매체가 관련 기사를 여러 건 쓴 것**이고,
  `hot_n_press` 가 과병합 판단 근거로 첫 배치에서 실제로 쓰였다
- **`business_relevance` 하한 0.42 → 0.34, 하위 구간 4건 → 13건.**
  바닥이 깔리기 시작했다. 다만 이슈 구성이 달라 **기사 수·매체 수 제거만의
  효과라고 단정하면 안 된다.** 캘리브레이션 종료 조건은 배치 3회다
- **압축률 62 → 51.** 복수 20건(32%)이 스파이크의 11/38(29%)과 비슷하다.
  v1 이 43개 중 42개를 singleton 으로 만든 실패는 재발하지 않았다
- **소재 51건을 사람이 주 1회에 다 볼 수 있나.** 7단계 임계가 필요해지는
  실무적 이유다

---

## 2026-08-26 (오후) — 4단계 분석

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 116 | **4단계 본문에 상한을 두지 않는다.** 전문을 보낸다 | 실측 66건에서 **상한이 필요한 증거가 없었다.** 31,097자 → summary 223자, 26,902자 목차형 Q&A → `CCS 역할·경제성 재평가`. `summary_warn` 0 · `signal_rejected` 0 · 본문↔summary 상관 Pearson 0.28. 3단계는 실패가 관측돼서 600자 규칙에 근거가 있었지만(ESG Today 106자), 여기는 길다는 사실만 있었다. **유일한 실패 1건은 2,104자라 상한과 무관하다** — 지금 상한을 두면 관측된 문제를 해결하지도 않으면서 정보만 버린다 | 4단계 |
| 117 | **상한 판단을 첫 실행으로 미룬 것 자체가 결정이었다** | 지금 상한을 두면 **첫 실험을 해석할 수 없다** — signal 이 나빠도 "원문이 길어 흐려진 것"인지 "잘려서 핵심이 사라진 것"인지 구분이 안 된다. 상한 없는 실행이 "모델이 원문 전체를 받았을 때 어디까지 압축하는가"의 기준선이고, 그게 있어야 나중에 상한의 효과를 비교할 수 있다 | 4단계 |
| 118 | **실패 행만 UPDATE 한다.** 성공 행은 UNIQUE 가 지킨다 | `INSERT OR IGNORE` 로 두면 **재시도가 조용히 무시된다** — failed 행이 남아 다음 배치에서 또 잡히고 영원히 반복된다. `attempts` 도 안 오른다(n8n 은 항상 1 을 보낸다). **`attempts` 컬럼과 `failed AND attempts < 3` 조회 조건이 둘 다 죽어 있었다** — 설계는 있는데 동작을 안 했다. 실패는 옛 버전을 보존할 이유가 없다: 튜닝 재료가 되는 건 성공한 분석이고 실패 행은 재시도 카운터일 뿐이다 | 4단계 |
| 119 | **`attempts` 는 collector 가 센다.** 요청 값을 믿지 않는다 | n8n 은 자기가 몇 번째 시도인지 모른다. DB 만 안다 | 4단계 |
| 120 | **`no_signal` 은 collector 가 판정해 응답에 담는다** | `issue_signal === null` 을 세면 **분석 실패까지 무이슈로 집계된다.** NULL 의 원인이 셋이다 — 무이슈(정상 출력) · 검증 거절 · 분석 실패. `empty_streak` 4분기·`reject_counts` 와 같은 계열이다 | 4단계 |
| 121 | **`subject_id` 불일치는 422.** 조회 시점과 저장 시점 사이 재분류를 막는다 | 분석은 subject 전제 아래 이뤄진다(#51). n8n 이 조회 시점 subject 로 프롬프트를 만들었는데 그 사이 재분류됐다면 **전제가 다른 분석**이다 | 4단계 |
| 122 | **분류·분석의 멱등성 방식이 다르다.** 분류는 4상태 조건부 갱신, 분석은 UNIQUE | 분류는 `articles` 를 UPDATE 해서 **덮어쓸 위험**이 있다(#98). 분석은 행을 추가하므로 안 넣기만 하면 된다. 원자성 단위가 다르면 장치도 다르다 | 3·4단계 |

### 검증한 것 (2026-08-26 오후)

| 항목 | 결과 |
| --- | --- |
| 저장 6종 | inserted · already_exists(다른 내용 보내도 안 덮음) · signal 30자 초과 거절(`too_long:34`) · subject 불일치 422 · done 인데 summary 없음 422 · 무이슈 null 저장 |
| 재시도 3종 | `retried` + attempts 2 · 재조회 0건 · **done 행은 안 덮어씀** |
| 실전 66건 | done 65 · failed 1(파서 형식) · `signal_rejected` 0 · `summary_warn` 0 |
| `change_type` 분포 | 해설 24 · 동향 24 · 변화 17. **어느 하나로 안 쏠려 `n_change` 가 열도 신호로 살아 있다** — 6종에서 3종으로 줄인 #24 의 효과 |
| 데일리 3단계 | 셋 다 0건 경로로 완료 반환. 상위가 순서를 제어할 수 있다는 전제가 세 단계 모두에서 확인됐다 |

### 관찰 — 다음 배치에서 볼 것

- **`issue_signal` null 이 0건이다.** 스파이크에서는 evergreen 기사에 null 이
  나왔는데 66건 전부 signal 이 붙었다. `Explainer: How the super El Niño
  will…` 같은 기사도 받았다. 프롬프트의 "애매하다는 이유만으로 null 을 쓰지
  마라"가 잘 먹은 것인지, **null 을 안 쓰는 쪽으로 기운 것인지** 더 봐야 안다
- **파서 형식 실패 1.5%**(1/66). `Model output doesn't fit required format`.
  재시도로 회수됐지만 반복되면 스키마나 프롬프트를 본다
- **같은 subject 안에서 `issue_signal` 이 겹치는가.** 5단계가 묶을 수 있는지의
  선행 신호다 — 전부 제각각이면 클러스터링이 singleton 만 낸다

---

## 2026-08-26 — 워크플로 분리 · 데일리 자동 실행

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 109 | **워크플로를 셋으로 나눈다.** `00 데일리`(스케줄·순서) · `01 수집` · `02 분류`. 합치지 않는다 | **재실행 단위가 다르다.** 합치면 분류만 테스트해도 수집이 딸려 돌고, 새 `$execution.id` 라 `request_id` 가 달라져 멱등 히트가 안 걸린다 — 전건 duplicated 로 들어가 `empty_streak` 이 오른다. **그 신호를 지키려고 `ingest_requests` 를 만들었는데 테스트가 그걸 오염시키는 셈이다.** 반대로 수집만 확인하려다 LLM 82콜을 쓰는 것도 되돌릴 수 없다. 스케줄 두 개로 나누면 동시에 시작해 분류가 아직 안 들어온 기사를 대상으로 돈다 — 오프셋으로 때울 수 있지만 수집이 느려지면 깨진다 | 인프라 |
| 110 | **하위는 항상 완료 한 건을 반환한다.** zero 분기 + Merge + 단일 요약 | 상위가 순서를 제어하려면 하위가 빈 결과에도 완료를 말해야 한다. **`Run Once for All Items` 는 처리 모드지 빈 입력 실행 보장이 아니다** — `Split Out` 에서 흐름이 끊기면 downstream 이 아예 안 돈다. 그리고 **n8n 은 한 노드의 같은 입력에 여러 연결이 들어오면 브랜치마다 한 번씩 실행한다** — 네 갈래를 바로 물렸더니 요약이 여러 번 나오고 `processed` 가 76(=83−7)으로 digest 를 빠뜨렸다 | 인프라 |
| 111 | **요약은 실패를 버리지 않는다.** 원인별로 센다 | `POST ingest` 가 `onError=continueRegularOutput` 이라 실패해도 같은 출력으로 흐르는데 그 아이템은 `source_id` 가 없다. `filter` 로 걸러내면 실패한 소스가 사라지고 상위가 "정상 완료"로 읽는다. `due_sources`(GET 응답에서 직접) / `processed_sources` / `failed_sources` 를 나누고, 분류는 `format_rejected`(형식)와 `collector_rejected`(422·409)를 나눈다 | 인프라 |
| 112 | **게이트는 `【태그】` 화이트리스트.** `【` 전체를 거르지 않는다. 모르는 태그는 **통과** + 경고 | 실측 10건 중 3건(`【기획】` KSSB 전수 분석 ×2, `【칼럼】` 넷제로 전략)은 단일 주제라 오히려 좋은 소재 재료다. **`excluded` 는 다운스트림 전체에서 사라지므로 30% 오제외는 규칙 단순성으로 감수할 수 없다.** 잘못 거르면 기사가 통째로 사라지지만 잘못 통과시키면 분류가 이상해질 뿐이다 — 다만 **통과한 다이제스트가 반드시 `mapping_failed` 가 되는 건 아니라**(담긴 여러 건 중 하나를 골라 정상 subject 로 분류될 수 있다) 경고 건수를 요약에 남긴다 | 3단계 |
| 113 | **`digest_tags` 는 `tuning.yaml` 이 정본.** `GET /prompts/classify` 응답에 실어 보낸다. **해시에는 넣지 않는다** | n8n 코드에 배열을 다시 쓰면 정본이 둘이 된다 — 워크플로에서 프롬프트 복사본을 없앤 이유(#100)와 같은 문제다. 해시에 안 넣는 건 **프롬프트 내용이 아니라 실행 설정**이라, 이게 바뀌었다고 프롬프트 버전이 바뀐 건 아니기 때문이다(해시 `965531c5` 유지 확인) | 3단계 |
| 114 | **출력 파서에서 `subject_id` 의 `minimum`/`maximum` 제거** | 스파이크 스키마에 `1~13` 이 박혀 있었다. 그대로 두면 subject 개수가 사는 곳이 셋이 된다(`seed.sql`·프롬프트·파서). 존재 검증은 collector 가 `subjects` 조회로 한다(#99) | 3단계 |
| 115 | **실행 기록은 n8n 에 남긴다. `runs` 테이블은 안 만든다** | pruning 이 꺼져 있어 안 잘린다. 하루 1회 × 프롬프트 600KB 면 연간 수백 MB 고 `/mnt/data` 에 15G 여유가 있다. **실패가 실제로 어떤 모양인지 모른 채 스키마를 만들면 또 고친다** — "없는 기능을 위한 테이블"을 안 만든다는 원칙. **필요해지는 시점: 무인 운영에서 "지난주 어느 날 실패했나"를 물어야 할 때** | 인프라 |

### n8n 에서 배운 것

문서에 남기지 않으면 다음에 또 헤맨다.

| | |
| --- | --- |
| `Execute Workflow Trigger` | **`Input data mode` 를 골라야 한다.** 안 고르면 하위가 "이슈 있음"이 되고, 그걸 부르는 상위가 같은 에러를 낸다. 넘길 데이터가 없으니 `Accept all data` |
| Publish | **하위도 Publish 해야 상위가 호출할 수 있다.** "하위는 안 켜도 된다"고 판단했는데 틀렸다 |
| 타임아웃 | **실행 단위라 하위에도 걸어야 한다.** 하위는 자기 실행 기록을 따로 갖고 자기 설정을 쓴다 — 상위만 3600 이면 하위 기본값에서 잘린다. 인스턴스 상한 1시간 |
| 여러 연결 | 한 노드의 같은 입력에 여러 연결 = **브랜치마다 한 번씩 실행.** Merge 로 합쳐야 한 건이 나온다 |
| JSON 임포트 | 내가 손으로 쓴 워크플로 JSON 은 파라미터 구조가 인스턴스 버전과 어긋나 **필드가 빈 껍데기로 남는다.** 화면에서 만들고 Download 로 받는 게 정확하다 |

### 검증한 것 (2026-08-26)

| 항목 | 결과 |
| --- | --- |
| zero-due | 폴링 주기 대기 중 실행 → `due_sources 0` 으로 요약 한 건 도달 |
| due 있음 | `last_fetched_at` 을 지우고 실행 → 4곳 처리, 신규 1 · 중복 82 |
| `empty_streak` 4분기 | 실전에서 갈렸다 — 임팩트온만 신규가 있어 0 리셋 + `last_ok_at` 갱신, 나머지 셋은 +1 |
| 게이트 단위 6종 | 공백 정규화(`【 ESG  Deal 】`) · 중간 `【표현】` 무시 포함 전부 통과 |
| 게이트 실측 83건 | digest 7 · pass 76(모르는 태그 3: 기획 2 · 칼럼 1) |
| 분류 실전 83건 | mapped 58 · mapping_failed 18 · digest 7 · 실패 0. **13개 subject 전부에 기사가 붙었다**(0건 없음) |
| 데일리 전체 | 수집 02:12:51 → 분류 02:13:17. **26초 간격이 순서 보장의 증거** |

### mapping_failed 21.7% 를 어떻게 읽었나

프롬프트 문서의 "20% 를 넘으면 subjects 공백을 먼저 의심한다"에 걸렸지만
**갈라보면 13건이 노이즈다.**

```
도메인 밖              7   문화·연예·헬스케어 (환경미디어)
기업 M&A·인사·지배구조   6
다이제스트성            2
진짜 공백 후보          3   LDES · 기후 인식 연구 · 유해물질 규제
```

**프롬프트를 고쳐도 노이즈는 안 줄어든다** — `exclude_keyword`·`out_of_domain`
판정을 켜야 풀리고 그건 MVP 에서 미룬 결정이다. 공백 후보 3건은 서로 다른
덩어리라 새 subject 하나로 못 묶는다. **한 배치 더 쌓고 판단한다.**

같은 83건을 두 번 돌렸더니 59/17 → 58/18 이었다. **LLM 비결정성이고,
프롬프트 효과를 볼 때 "1건 차이는 노이즈"라는 기준이 생겼다.**

### 미결정

- **실패 알림(n8n Error Workflow).** 무인 운영에 들어갔으니 필요한데
  **알림 채널을 안 정했다** — 슬랙·이메일·텔레그램 중 뭘 쓸지. 채널 없이
  만들면 아무 데도 안 간다. 아침에 결과를 확인하는 습관이 생기면 그때
- **하위 둘의 타임존이 `None`.** 지금은 스케줄이 상위에만 있어 영향이
  없지만, `01 수집` 의 **비활성 스케줄 노드를 다시 켜면 UTC 로 돈다.**
  `Asia/Seoul` 로 맞추거나 그 노드를 아예 지운다
- **n8n 이 공인 IP 로 열려 있다**(`0.0.0.0:5678`, ufw 꺼짐). 워크플로에
  OpenAI 크레덴셜이 있다. 파이프라인이 다 돌면 정리한다

---

## 2026-08-25 (심야, 2) — 프롬프트 서빙

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 100 | **프롬프트 정본은 레포의 md. 워크플로 JSON 에 복사본을 두지 않는다.** collector 가 `GET /prompts/{name}` 으로 서빙한다 | `schema.sql` 과 마이그레이션 델타는 **둘 다 필요한 산출물**이지만(하나는 새 DB, 하나는 기존 DB), 워크플로 안의 프롬프트는 **없어도 되는 파생본**이다. 같은 취급을 할 이유가 없다. "자주 고치는 단계"는 복사본을 둘 이유가 아니라 **동기화 실패 가능성이 가장 큰 시기**라는 뜻이다 | 3~6단계 |
| 101 | **자리표시자는 `[[TITLE]]` 형식.** n8n 표현식(`{{ $json.title }}`)을 파일에 넣지 않는다 | HTTP 로 받아온 문자열 안의 표현식을 n8n 이 다시 평가해준다는 보장이 없다 — 리터럴로 박힌 채 LLM 에 간다. 중립 자리표시자면 정본은 레포에, n8n 에는 범용 렌더링 코드만 남아 **프롬프트를 고쳐도 워크플로를 안 건드린다** | 3~6단계 |
| 102 | **프롬프트에 `@lru_cache` 를 걸지 않는다.** 대신 n8n 이 **실행 시작에 한 번만** 가져간다 | 작은 md 를 실행당 한 번 읽는 비용은 무시할 수준이고, 캐시를 걸면 고칠 때마다 재시작해야 한다. 캐시가 필요한 건 배치 도중 값이 바뀌면 안 되는 `tuning.yaml` 이고, 프롬프트는 **한 번 받아 참조**하면 그 요구가 충족된다 — 기사마다 가져오면 실행 도중 파일이 바뀔 때 같은 배치가 서로 다른 프롬프트를 쓴다 | 3~6단계 |
| 103 | **프롬프트·DB subject 대조를 `GET /prompts/classify` 에서 요청마다** 한다. 시작 검증이 아니다 | **#102 가 검증 위치까지 정한다** — 캐시가 없으니 서비스가 뜬 뒤 `system.md` 가 수정되면 시작 검증은 통과한 상태로 잘못된 프롬프트를 계속 서빙한다. 그렇다고 시작 검증으로 서비스를 못 뜨게 하면 **분류 프롬프트 하나 때문에 `/ingest` 까지 멈춰 수집도 못 한다.** 영향 범위가 맞는 곳에서 막는다 — 실측으로 `/prompts/classify` 500 · `/sources/due` 200 확인 | 3단계 |
| 104 | **검사 넷 — 중복 id · 누락 · 추가 · 이름 불일치.** 여러 문제는 한 번에 다 보고한다 | `dict` 로 바로 만들면 **중복 id 가 조용히 덮어써진다.** 하나 고치고 다시 돌려 다음 걸 찾는 건 느리다 | 3단계 |
| 105 | **해시에 파일 경계를 넣는다.** 파일명·NUL·길이(8B big-endian)·내용 순 | 바이트만 이어 붙이면 `system="ab"+user="c"` 와 `system="a"+user="bc"` 가 같은 `abc` 라 **같은 해시가 된다**(실측 확인). 실제 분류에 쓰기 전이라 지금 바꿨다 | 3~6단계 |
| 106 | **해시는 식별 신호이지 불변성 강제가 아니다.** "쓰인 버전은 고치지 않고 v5 를 만든다"는 규칙을 막아주지 않는다 | 구분해두지 않으면 "해시가 있으니 안전하다"고 오해한다. 같은 버전인데 해시가 달라졌다면 규칙이 깨진 것이고, 그걸 **알아차릴 수단**일 뿐이다 | 3~6단계 |
| 107 | **`system.md` 문법 규칙 — `### 숫자. 이름` 은 subject 정의에만 쓴다** | `prompt_validation` 이 이 형식을 정규식으로 뽑는다. 다른 절에 번호를 매기면 subject 로 잡혀 "DB 에 없는 id" 로 막힌다. 정규식이 프롬프트 문법에 의존한다는 사실 자체를 문서에 남긴다 | 3단계 |
| 108 | **`tests/` 신설.** 화면에서 한 번 돌린 검증은 레포에 남긴다 | 일회성 검증은 다음에 코드를 고칠 때 아무것도 지켜주지 않는다. 특히 **운영 파일과 DB 를 직접 대조하는 테스트**가 `seed.sql` 과 프롬프트 중 하나만 고쳤을 때 잡아준다 | 전체 |

### 정정 — 분류 입력량

`GET /articles/pending-classify` 의 **53,086자는 기사 부분만**이다. 여러 번
"총 입력"이라고 적었는데 틀렸다. System Prompt 가 기사마다 다시 들어간다.

```
System Prompt   6,753 × 82  =  553,746자
User 고정 레이블    ~60 × 82  =    4,920자
기사 입력                    =   53,086자
                              611,752자      ← 기사 부분은 8.7%
```

프롬프트를 실행당 한 번 가져오는 것은 **HTTP 조회를 줄일 뿐 LLM 입력을
줄이지 않는다.** 600자 상한(#94)도 마찬가지다. "필터는 싸게"(#18)가 여전히
맞는지는 **4단계와 비교해야** 안다 — 분석은 본문 전문을 보내므로 System
Prompt 비중이 훨씬 낮다. 첫 실행 후 실제 토큰으로 확인한다.

### 검증한 것 (2026-08-25 심야)

| 항목 | 결과 |
| --- | --- |
| `prompt_validation` 단위 9종 | 정상·중복·누락·추가·이름불일치·여러문제·공백 견딤·subject 아닌 제목 무시·**운영 파일과 DB 직접 대조** |
| 영향 범위 | DB 이름을 어긋나게 두면 `/prompts/classify` 500 · `/sources/due` 200 |
| 해시 경계 | 옛 방식은 `ab`+`c` 와 `a`+`bc` 가 같은 해시(`ba7816bf…`), 새 방식은 다름 |
| 프롬프트 서빙 | sha256 이 파일 직접 계산과 일치 · 자리표시자 4개 · 표현식 잔재 0 · 404/500 |
| 운영 DB 를 건드리는 테스트 | `try/finally` 로 원복. 중간에 끊겨도 잘못된 값이 안 남는다 |

---

## 2026-08-25 (심야) — 3단계 착수

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 91 | **`state_reason` 에 `digest` 추가** (마이그레이션 003, user_version 3). `state='excluded'` 강제 | 게이트가 AI 호출 전에 거르는데 판정을 저장하지 않으면 다음 조회에 또 나온다 — 매 배치마다 같은 기사를 다시 걸러내는 걸 영원히 반복한다. `non_analyzable`("채용·행사·세미나·인사")을 재활용하면 집계에서 갈라볼 수 없다. 분류 실패가 아니라 **기사 단위로는 분석할 수 없다는 확정 판정**이라 `active` 로 남기면 이후 모든 조회가 예외 처리를 반복한다 | 3단계 |
| 92 | **게이트 판정을 collector 로 옮긴다.** n8n 에는 `digest` 판정만 남는다 | `not_active`·`no_body` 는 조회 조건이라 애초에 결과에 안 나오고, id 검증은 저장 시점에 `subjects` 와 대조하면 된다. **게이트 노드의 `SUBJECTS` 상수가 사라져 동기화 지점이 셋에서 둘로 준다** — 프롬프트 문서에 "DB 가 붙으면 조회로 대체"라고 적어둔 그 시점이다. `digest` 만 n8n 에 남는 이유는 **AI 호출 전에** 걸러야 해서다 — collector 로 보내봤자 이미 콜을 쓴 뒤다 | 3단계 |
| 93 | **id↔name 불일치를 `mapping_failed` 로 강등하지 않는다.** 저장하지 않고 422 + 로그 | **#19 를 뒤집는다.** `mapping_failed` 는 "subjects 목록의 공백"을 찾는 신호인데 모델의 형식 오류를 섞으면 그 신호가 죽는다. `empty_streak` 에서 죽은 피드와 정규화 오류를 가른 것, `rejected` 를 따로 센 것과 같은 계열이다. 저장 안 하면 다음 조회에 다시 잡히므로 재시도가 자연히 된다 | 3단계 |
| 94 | **분류 입력 = `title` + `classification_text`(body 앞 600자, 없으면 snippet)** | **#18 을 부분 수정한다.** #18 의 근거는 "본문 32만 자를 필터에서 또 읽으면 비용 2배"였는데 600자 상한이 그 근거를 없앤다. 실측에서 `snippet` 이 소스별로 18배 차이 났다 — 환경미디어 1,898자(description 에 전문) vs ESG Today 106자(description 이 대부분 `<img>`). 자르기만 하면 위쪽만 정리되고 ESG Today 는 제목 말고 판단할 게 없다. 정신(싸게 전건, 비싸게 통과분만)은 유지되고 구현만 바뀐다 | 3단계 |
| 95 | **설정 검증을 프로세스 시작 시점에.** `config.validate()` | `classification` 키가 없어도 systemd 는 `active` 였고 엔드포인트만 500 을 냈다. 늦게 터지면 "서비스는 살아 있는데 요청만 실패"라 원인을 찾기 어렵다 | 인프라 |
| 96 | **응답 필드명을 `classification_text` 로.** `snippet` 이라 부르지 않는다. `text_truncated` 를 같이 준다 | 저장된 원문과 잘린 값을 같은 이름으로 부르면 혼동한다. SQLite 에 불린이 없어 비교 결과가 0/1 로 오므로 응답에서 `bool` 로 바꾼다 — n8n 의 `=== true` 가 실패한다 | 3단계 |

### 검증한 것 (2026-08-25)

| 항목 | 결과 |
| --- | --- |
| 마이그레이션 003 | 82건 보존 · FK 무결 · 인덱스 5개 복원 · `active+digest` 조합 거부 |
| `schema.sql` ≡ 델타 적용본 | 구조 일치. 유일한 차이는 `RENAME` 이 붙이는 테이블명 따옴표 |
| fallback 8종 | body 700/600/601 · body NULL·공백 + snippet · 둘 다 없음·공백 · snippet 50자 — 전부 의도대로 |
| `limit` 경계 | 0·-1·501 → 422 · 500 → 200 |
| 시작 검증 | `input_max_chars: 0` 에서 서비스가 뜨지 못함 |
| Carbon Brief UI 잔재 | 600자 전체 스캔에서 0건 |

---

## 2026-08-25 (밤, 2) — /ingest 구현 중 뒤집은 것

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 88 | **기사를 요청 모델(`ArticleIn`)이 아니라 `dict` 로 받는다.** 개별 검증은 `ingest` 안에서 기사 단위로 하고 `rejected` 로 센다 | 모델로 받으면 FastAPI 가 **요청 파싱 단계에서** 검증해, 한 건만 어긋나도 요청 전체가 422 가 된다. 임팩트온 50건 중 1건 때문에 49건이 같이 버려지고 **다음 폴링에서 또 반복된다.** #87 이 `rejected` 를 계약에 넣어놓고 정작 도달할 수 없는 코드를 만들었다 | 1단계 |
| 89 | **`reject_counts` 에 필드와 사유를 담는다** (`url_key:value_error` 형태) | 숫자만 있으면 n8n 정규화의 **어디를** 고쳐야 할지 모른다. `empty_streak` 을 원인별로 가른 것과 같은 이유 — 다른 원인이 같은 신호를 내면 안 된다 | 1단계 |
| 90 | **`received` 는 `req.articles` 의 길이 그대로.** rejected 를 더하지 않는다 | #88 로 rejected 된 기사도 `req.articles` 안에 들어오게 됐다. 계약(`received = inserted + duplicated + rejected`)은 그대로 성립한다 | 1단계 |

### 검증한 것 (2026-08-25)

`/ingest` 의 모든 분기를 실제 호출로 확인했다.

| 분기 | 결과 |
| --- | --- |
| 멱등 히트 | 저장된 응답 반환 · `last_ok_at` 도 안 움직인다 · articles·requests 개수 불변 |
| `payload_hash` 불일치 | HTTP 409 `idempotency_key_reused` |
| `fetch_ok=false` | streak 유지 · `last_error='timeout'` |
| 빈 피드 ×2 | streak 0 → 1 → 2 |
| 신규 1건 | streak 0 리셋 · `last_error` NULL 로 정리 |
| 전건 rejected | **streak 2 유지** · `last_error='validation_rejected:2'` |
| 정상 1 + 깨진 2 혼합 | HTTP 200 · `received 3 · inserted 1 · rejected 2` — 정상 건이 살아남는다 |
| `published_at` 없음 | `state='excluded'` 로 **저장** · `active_ids` 에는 안 들어감 |
| `body_state` 판정 | 본문 있음 → `full` · 없음 → `pending` |
| `press` | n8n 이 안 보내도 `sources.press` 에서 채운다 |

---

## 2026-08-25 (밤) — collector 착수 · 마이그레이션 002

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 77 | **collector 는 호스트 systemd 서비스.** `127.0.0.1:8787` 에만 바인딩 | n8n 이 컨테이너가 아니라 호스트 프로세스(systemd)라 loopback 을 공유한다. 컨테이너 경계·게이트웨이 IP·방화벽 규칙이 전부 불필요해지고, **외부에서 도달 자체가 불가능**하다 — 설정 실수로 열릴 여지가 없는 게 제일 좋은 상태다 | 인프라 |
| 78 | **`last_fetched_at` 은 `/ingest` 가 찍는다.** `/sources/due` 는 읽기 전용 | `/sources/due` 에서 찍으면 실패한 수집도 성공처럼 시각이 남아 다음 주기까지 재시도가 막히고, `empty_streak` 이 죽은 피드를 잡는 신호도 흐려진다. 쓰기가 한 곳으로 모여 트랜잭션 경계도 단순해진다 | 1단계 |
| 79 | **정규화는 n8n 이, 저장·검증은 collector 가.** | 정규화가 이미 82건 실측으로 다듬어져 있다(url_key 의 utm 제거, 임팩트온 tz_offset). 지금 파이썬으로 옮기면 **검증된 코드를 근거 없이 다시 쓰는 것**이다. 크롤 어댑터를 만들 때 소스마다 갈리면 그때 옮긴다. 단 collector 는 받은 걸 그대로 믿지 않는다 — DB 소유자가 검증도 소유한다 | 1단계 |
| 80 | **`ingest_requests` 테이블 신설 (마이그레이션 002, user_version 2)** | 재시도로 `empty_streak` 이 잘못 +1 되어 살아 있는 피드가 죽은 것으로 집계된다. `articles` 에 흔적을 남기는 방식은 기사 행이 없는 요청(빈 피드·전건 rejected·fetch 실패)을 못 기록하고, `sources.last_request_id` 는 그다음 요청 이후 도착한 재시도를 못 잡는다 | 1단계 |
| 81 | **`payload_hash` 를 같이 저장.** 같은 `request_id` 에 다른 내용이면 409 | 워크플로를 고쳐 다시 돌리면 같은 키에 다른 내용이 온다. 저장된 옛 응답을 돌려주면 조용히 틀린 답이 된다 | 1단계 |
| 82 | **`/ingest` 는 `BEGIN IMMEDIATE`** | SQLite 기본 `BEGIN` 은 deferred 라 첫 쓰기까지 잠금을 안 잡는다. 동시 도착한 같은 요청 둘이 나란히 "없음"으로 판정하고 둘 다 진행한다 | 1단계 |
| 83 | **`fetch_ok` = HTTP 성공 **AND** 피드 파싱 성공** | 200 인데 XML 이 깨진 경우를 `true + articles=[]` 로 보내면 죽은 피드로 오인된다. 개별 기사 정규화 실패는 `true` + 그 기사만 rejected | 1단계 |
| 84 | **`empty_streak` 규칙 4분기** — fetch 실패는 유지 · 신규 1건 이상(excluded 포함)은 0 · 전부 중복이거나 피드 0건은 +1 · **전건 rejected 는 유지** | 전건 rejected 를 신규 0건으로 세면 n8n 정규화 오류가 죽은 피드로 오인된다. `excluded` 로 저장된 기사도 신규다 — 발행일이 없어도 새 기사가 왔다는 건 피드가 살아 있다는 뜻이다 | 1단계 |
| 85 | **`body_state` 는 collector 가 판정.** body 있으면 `full` · 없고 `body_mode='snippet'` 이고 snippet 있으면 `snippet_only` · 그 외 `pending`. 공백 문자열은 없는 것으로 본다 | n8n 이 보낸 값을 믿으면 본문이 비었는데 `full` 로 오는 걸 못 막는다 | 1단계 |
| 86 | **`articles.press` 는 `sources.press` 를 정본으로.** 전달값이 다르면 경고 | 결정 #9 의 연장 — press 는 수집 시점의 사실이고, 그 사실의 출처는 `sources` 다 | 1단계 |
| 87 | **응답 숫자의 포함 관계를 명시.** `received = inserted + duplicated + rejected` / `inserted = active + excluded`. n8n 에는 `active_ids` 를 준다 | 명시 안 하면 `excluded` 가 `inserted` 밖으로 읽혀 합이 안 맞는다. `excluded` 기사를 다음 단계로 넘기면 분류 콜을 낭비한다 | 1·3단계 |

---

## 2026-08-25 (저녁) — 2차 리뷰 반영

리뷰 지적 6건 전부 수용. #59(requeue)를 만 하루도 안 돼 뒤집었다.

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 67 | **requeue 를 MVP 에서 제외.** #59 철회. `requeued_at` 컬럼과 `tuning.yaml` 의 requeue 설정 삭제 | 마커를 되돌려도 **다음 배치엔 이미 창 밖이라 곧바로 stale 로 죽는다** — 동작이 완성되지 않았다. 더 근본적으로 `too_broad` 는 "다시 수집하라"가 아니라 **"이 이슈를 쪼개라"** 다. 필요한 건 재수집이 아니라 재분할이고, 그 경로를 설계하기 전에 컬럼만 만들면 반쯤 작동하는 기능이 남는다 | 5단계 |
| 68 | **stale UPDATE 에 `collected_at <= :as_of` 추가** | 후보 조회에만 넣고 여기엔 안 넣어 경계가 한쪽만 닫혀 있었다. 배치가 도는 동안 늦게 수집된 과거 기사까지 이 배치가 삼킨다 | 5단계 |
| 69 | **후보 조회에 `published_at < :window_to` 상한 추가** | 미래 시각으로 잘못 파싱된 기사가 후보에 낀다. 실측 확인 | 5단계 |
| 70 | **초기화 순서에 `PRAGMA foreign_keys=ON` 을 트랜잭션 밖으로 명시** | 트랜잭션 안에서는 **no-op 이라 에러 없이 조용히 0 으로 남는다**(실측). journal_mode 와 달리 실패가 안 보여서 더 위험하다 | 문서 |
| 71 | **관계 표기 정정** — `소재 1 : N 기사` / `기사 N : 1 소재` | "기사 → 소재 1:N" 은 기사 하나가 여러 소재에 들어간다는 반대 뜻이다. #57 을 쓰면서 표기를 틀렸다 | 문서 |
| 72 | **`state_tags` 어휘를 영문 스네이크로 고정** + `low_heat` 신설. `tuning.yaml` 의 `topic_reject_tags` 가 정본 | `SCHEMA.md`(한글 공백)와 `tuning.yaml`(한글 언더스코어)이 갈려 있어 설정 조회가 실패할 상태였다. `state_reason` 결정(#52)과 같은 이유로 영문 통일 | 8단계 |
| 73 | **`sources.poll_interval_min` 의 `DEFAULT 720` 제거.** collector 가 `tuning.yaml` 값을 넣는다 | YAML 만 고치면 DDL 기본값이 그대로 남아 둘이 갈린다. #65 의 단일 출처 원칙이 여기서 새고 있었다 | 1단계 |
| 74 | **캘리브레이션 종료 조건을 상대 기준으로 교체** — "`business_relevance` 최솟값 0.2 미만"(#66) 폐기 | 3단계가 이미 서비스 관련 subject 로 걸러낸 뒤라 0.2 미만이 **영구히 안 나올 수 있다.** 종료 조건이 아니라 영구 정지 조건이 된다. `service_mismatch` 거절분이 `kept` 보다 낮은가로 바꿨다 | 6·7단계 |
| 75 | **`low_heat` 판정 태그로 열도를 따로 받는다** | `kept`/`rejected` 만 보면 서비스 직접성·근거 부족·중복이 섞여서 `hot_*` 가 맞는지 분리해 확인할 수 없다 | 7단계 |
| 76 | **`topics` 는 완성된 소재만 담는다.** `subject_id`·`business_relevance`·`hot_*`·`n_citable` NOT NULL, `json_array_length(article_ids) > 0` | 근거 0건·점수 NULL·열도 전부 NULL 인 행이 통과하고 있었다. 미완성을 남기려면 `pending` 상태를 먼저 정의해야 한다 — NULL 로 표현하면 "아직 안 됨"과 "계산했더니 0"이 같은 값이 된다 | 8단계 |

---

## 2026-08-25 (오후) — 외부 리뷰 반영

리뷰 지적 8건 중 5건 수용, 3건 반박. 수용분은 아래, 반박 근거는 해당 문서에.

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 56 | **"batch_id 가 배치 내 중복도 막는다"는 서술 철회.** 배치 간만 막는다. 배치 내 보장자는 5단계 완전 분할 불변식(프롬프트 + 검증 코드) | 리뷰 지적이 맞다. 같은 기사 id 를 여러 `topics.article_ids` 에 넣는 것을 스키마가 막지 않는다. **DB 가 아니라 파이프라인이 보장한다**를 명시 | 문서 |
| 57 | **기사↔소재 관계를 1:N 으로 정정.** v1 의 "진짜 N:M(관통 소재)" 서술 폐기 | 5단계가 한 기사 한 이슈를 강제하고 배치가 기사를 소비한다. N:M 이 되는 경로는 "핵심 기사 vs 보강 기사" 정책뿐이고, 그건 도입한 적 없다 | 문서 |
| 58 | **`topic_articles` 는 계속 안 만든다.** 되살아나는 트리거 = 보강 기사 재사용 정책 도입 | #57 이 근거다. 1:N 이면 연결 테이블이 주는 FK·중복 방지·역할 구분이 남는 값을 못 만든다 | 5·8단계 |
| 59 | **`requeue` 를 명시적 동작으로 정의.** `batch_id`·`consume_reason` 을 NULL 로 되돌리고 `requeued_at` 을 찍는다. 태그별 정책은 `tuning.yaml` | `state_tags` 표는 "너무 넓음 → articles 그대로"라는데 소비 마커 때문에 실질적으로 사라졌다. **표와 동작이 모순**이었다. stale 기사의 "나중에 재사용" 서술도 자동 경로가 없었다 | 5단계 |
| 60 | **후보 조회에 `collected_at <= as_of` 추가** | 배치 실패 후 재실행 사이에 과거 발행 기사가 늦게 수집되면 후보 집합에 새로 낀다. `as_of` 만으로는 재현이 안 됐다 | 5단계 |
| 61 | **`as_of` = 배치 시작 UTC 시각으로 통일** | 문서마다 `max(collected_at)` 과 실행 기준시각으로 갈려 있었다. `max(collected_at)` 은 DB 없는 스파이크의 임시방편이다 | 5단계 |
| 62 | **`migrations/001_init.sql` 삭제.** 최초 스키마에 해당하는 마이그레이션 파일을 두지 않는다. 델타는 002 부터 | 그 파일만 적용하면 **테이블 0개인 빈 DB 가 정상 v1 로 보인다.** DDL 중복을 피하려다 판 함정 | 문서 |
| 63 | **초기화는 스키마 + 시드 + `user_version` 을 한 트랜잭션으로.** `journal_mode` 만 트랜잭션 밖 | 나누면 시드 실패 시 subjects 가 빈 DB 가 "정상 v1" 로 남는다. WAL 전환은 트랜잭션 안에서 거부된다(실측) | 문서 |
| 64 | **CHECK 보강** — `json_type='array'`, `attempts>=1`, `prompt_version>=1`, `hot_*>=0`, `final_score 0~1`, done 이면 `summary`·`change_type` NOT NULL, `window_from < window_to`, 판정 상태면 `decided_at` NOT NULL | "정본"이라면서 빈 `done`, 배열 아닌 `article_ids`, 음수 열도가 통과했다 | 전체 |
| 65 | **값을 `tuning.yaml` 로 옮긴다. `TUNING.md` 에는 근거만** | "이 파일만 고친다"고 써놓고 "코드 상수도 함께 고친다"고 적은 자기모순. 코드가 읽는 파일이 정본이어야 진짜 단일 출처다 | 문서 |
| 66 | **캘리브레이션 종료 조건 명문화** — 배치 3회 + `business_relevance` 하한이 0.2 아래 + `hot_*` 와 사람 판정의 상관 | 임계 미적용 상태에 끝이 없으면 영영 캘리브레이션이다. 현재 상태는 "핫한 소재 선별"이 아니라 **선별 기준을 만들기 위한 수집 구조**임을 문서에 명시 | 6·7단계 |

### 반박한 지적 셋

| 지적 | 왜 반박했나 |
| --- | --- |
| `can_cite=0` 기사의 흔적이 사라진다 | 사라지지 않는다. `topics.article_ids` 는 클러스터 구성 기사 **전체**를 담고 `n_citable` 도 저장한다. 근거 배열에서 빠지는 건 독자에게 보여줄 링크 목록이다 |
| 생성 당시 `can_cite` 를 스냅샷하라 | 결정 #9 와 충돌. press 는 **사실**이라 복사하고 can_cite 는 **정책**이라 조인한다. 스냅샷하면 임팩트온이 유료를 풀어도 과거 소재의 근거가 안 열린다 |
| `final_score = business_relevance` 를 CHECK 으로 | 열도를 섞을 예정이라 바뀔 값이다. SQLite CHECK 수정은 테이블 재생성 — 어댑터 문서에서 이미 같은 이유로 기각한 논거다 |
| `state_tags` 어휘를 CHECK 으로 고정 | JSON 배열 원소는 CHECK 에서 `json_each` 를 못 써 검사 불가. collector 검증으로 간다 (부분 반박) |

---

## 2026-08-25 (오전) — DB 착수


| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 40 | **collector 단일 창구.** DB 는 collector 만 연다. n8n·blogstudio 는 HTTP 로만 붙는다 | 스키마 v2 의 "n8n 이 쓰는 동안 blogstudio 가 읽는다"(08-19)와 어댑터 v1 의 "collector 가 DB 유일 소유자"(08-23)가 정면으로 모순이었다. 후자로 정리 | 전체 |
| 41 | **SQLite 유지.** #40 의 결과 | 쓰기 주체가 하나면 SQLite 의 유일한 실질 약점(동시 접근)이 사라진다. 재검토 트리거 둘을 SCHEMA.md 에 명시 — 임베딩 pre-filter 필요 시점, 쓰기 주체가 둘이 되는 시점 | 전체 |
| 42 | **수집·분석은 매일, 소재 생성은 주 1회(월요일).** | 기사 단위로 닫히는 일은 도착 순서와 무관하게 결과가 같다. 여러 기사를 가로지르는 일만 시간창이 필요하다. 부수 효과로 열도 신호에 변별력이 생긴다 | 1~8단계 |
| 43 | **배치는 미소비 기사만 가져간다.** `articles.batch_id IS NULL` 이 후보 조건 | 배치끼리 기사 집합이 안 겹친다 → 같은 기사가 두 소재의 근거가 되는 일이 구조적으로 불가능 | 5~8단계 |
| 44 | **`topics.issue_signal` 문자열 중복 회피 폐기.** 기록용 필드로만 남긴다 | #43 이 대체한다. LLM 이 매번 새로 짓는 30자를 비교하는 방식이라 애초에 잘 안 붙었다 — v2→v3 에서 같은 묶음의 이름이 실제로 바뀌었다 | 5·8단계 |
| 45 | **이슈 반복 허용.** 다른 기사로 같은 흐름을 다시 쓰는 것을 막지 않는다. 별도 장치 없음 | 근거가 다르면 중복이 아니라 후속이다. 6단계 입력에 최근 topics 를 주입하는 안은 접었다. 반복이 거슬리면 사람이 `rejected` 로 거른다 | 6단계 |
| 46 | **창(7일) 밖 미소비 기사는 소비 처리하고 후보에서 뺀다.** `consume_reason='stale'` | 안 찍으면 매주 재검사되며 영원히 안 뽑히고 조회만 무거워진다. 분석은 보존되므로 subject 확장·창 확대 시 재사용된다. 기사 소비의 목적이 이슈 탐색이고, 일주일이 지나면 이슈가 아니다 | 5단계 |
| 47 | **소비 마커는 배치 끝에 `topics` 저장과 같은 트랜잭션에서 찍는다** | 시작할 때 찍으면 중간에 터진 배치의 기사가 영구 증발한다. 끝에 찍으면 실패 시 `NULL` 로 남아 재실행이 그냥 된다 — 멱등성이 공짜 | 5~8단계 |
| 48 | **`batches` 테이블 신설.** `as_of`·창·상태·집계 | 열도가 실행 시각 기준이면 재실행마다 결과가 바뀐다. 기준시각을 못 박아야 재현된다 | 5단계 |
| 49 | **열도 신호를 `topics` 에 저장한다.** `hot_*` 컬럼 | "합산 공식은 후보를 눈으로 본 다음"이라고 미뤄놓고 저장을 안 하면 볼 게 없다. 스파이크 33건의 열도는 이미 휘발됐다 | 6단계 |
| 50 | **`issues` 테이블은 안 만든다.** 되살아나는 트리거 = 7단계 임계를 켜는 시점 | 6단계가 임계 없이 전 이슈를 소재화하므로 지금은 `이슈 ≡ topics 1행`. 임계를 켜면 탈락 이슈가 저장 자리를 잃는다 | 5·7단계 |
| 51 | **`article_analysis` 에 `subject_id` 스냅샷 추가.** UNIQUE 를 `(article_id, prompt_version, subject_id)` 로 | 3단계는 버전을 저장하지 않고 덮어쓴다. 재분류로 subject 가 옮겨가면 옛 전제로 만든 분석인지 알 수 없고, 기존 UNIQUE 로는 재분석도 막혔다 | 4단계 |
| 52 | **`state_reason` 어휘를 영문 스네이크로 통일.** 사람의 자유 서술은 `state_note` 로 분리 | CHECK 값 집합에 한글 문장이 섞이면 오타가 조용히 통과한다 | 1·3단계 |
| 53 | **`sources.content_tag` 칼럼 제거 → `config.rss.content_tag`** | rss 어댑터만 읽는 값이다. 어댑터 v1 의 판정 기준(여러 어댑터가 해석 = 칼럼, 단일 어댑터 = config) 적용. DB 가 없는 지금이 고칠 시점 | 1단계 |
| 54 | **문서 구조를 정본/근거/이력/수치로 분리.** `db/` 아래 `schema.sql`·`seed.sql`·`SCHEMA.md`·`DECISIONS.md`·`TUNING.md` | 같은 사실이 여러 곳에 살아서 이미 두 곳이 어긋나 있었다(`published_at` 제약, `content_tag` 위치). 실행 가능한 것을 정본으로 삼으면 드리프트가 물리적으로 불가능하다. **근거 문서는 컬럼 표를 다시 그리지 않는다** | 문서 |
| 55 | **확정스펙 xlsx 를 접는다.** DB 관련 내용은 전부 `db/` 로 흡수 | "정본은 시드인데 사람은 xlsx 를 본다"가 4·5번 subject 이름이 틀어진 경로 그대로였다 | 문서 |

---

## 2026-08-23 — 스파이크 전 구간

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 30 | 다이제스트(【】·핫클립)는 게이트가 제목 패턴으로 사전 제외 | 셀 수 있는 건 코드가 센다. LLM 에게 판정시킬 일이 아니다 | 3·4단계 |
| 31 | 4단계 `issue_signal` 은 문자열 또는 **JSON null**. 파서 스키마도 `["string","null"]` | 현재 사건에 연결되지 않는 evergreen 기사에 이슈를 강제하면 가짜 해설 클러스터가 생긴다. 프롬프트만 열고 스키마를 안 열면 정상 null 이 파서에서 터진다 | 4단계 |
| 32 | 검증기에서 **JSON null(정상)과 빈 문자열(형식 실패)을 분리 집계** | 섞어 세면 프롬프트 튜닝 신호가 오염된다 | 4단계 |
| 33 | 5단계 이슈 정의를 2유형으로 — ① 같은 사건의 복수 보도 ② 독립 사건들이 같은 변화 방향·압력·쟁점을 직접 뒷받침하는 묶음 | v1 의 "사건" 단일 정의가 43개 중 42개를 singleton 으로 만들었다. 열도의 핵심 신호가 전부 1·1·0 으로 죽었다 | 5단계 |
| 34 | 5단계에 **7일 시간창 필터** + `as_of`(=max collected_at) 계산·전달 | Carbon Brief 피드에 3주치가 남아 44건 중 6건이 창 밖이었다. 실행 시각 기준이면 재실행마다 열도가 바뀐다 | 5단계 |
| 35 | 접속사 합성 가드 — 서로 다른 변화 축을 나열해 한 문장을 만들 수 있다는 것만으로는 같은 이슈가 아니다 | v2 실측 `a09+a25` 과병합. 접속사로 이으면 무엇이든 한 문장이 된다 | 5단계 |
| 36 | 6·7·8단계를 한 콜로 합치고 **선별 임계는 미적용** | 임계를 정하려면 탈락선 아래까지 봐야 한다 | 6~8단계 |
| 37 | `business_relevance` 를 **순수 서비스 직접성 축**으로 좁힘. 기사 수·매체 수·최신성·화제성·예상 검색량 반영 금지. 생성물이 아니라 **입력 이슈**를 평가 | 화제성은 코드가 세고 검색량은 NULL 인데 모델이 추정하면 축이 침범된다. 합본의 자기 산출물 편향도 누른다 | 6단계 |
| 38 | **관통 가산(+0.1/+0.2)과 열도 합산 제거.** `final_score = business_relevance` | "합산 공식은 후보 실측 후"라는 직전 결정과 한 턴 만에 자기모순이었다. 점수 가중치는 검증 임계와 달리 순위를 조용히 비튼다 | 6단계 |
| 39 | 어댑터 판정 기준 — **여러 어댑터·공통 단계가 해석하는 값 = 칼럼, 단일 어댑터만 해석하는 값 = config 네임스페이스** | 목표 상태: 소스 추가 = 행 하나, 코드 변경 0. `kind × body_mode` 조합 검증은 DDL 이 아니라 collector 가 한다 | 1단계 |

---

## 2026-08-22 — subjects 개정

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 26 | **subject 4 확장** — `제품 탄소발자국과 전과정평가` → `탄소 산정과 전과정평가` | 산림 탄소회계(GHG 프로토콜) 기사가 슬롯이 없어 1번으로 흡수됐다. 기업 인벤토리·산정 방법론은 핵심 서비스 영역이라 `mapping_failed` 로 버리지 않는다. "공시(1) vs 산정(4)" 축 분리 | 3단계 |
| 27 | **subject 5 확장** — `탄소크레딧과 탄소제거` → `탄소크레딧·탄소제거·CCUS` | 배출원 CCUS 기사가 5번에 억지로 들어오고 있었다. `탄소시장` 으로는 안 넓힌다 — 배출권 할당·ETS 가격까지 빨아들인다 | 3단계 |
| 28 | 경계표에 `1 vs 4` · `5 vs 6` 추가 | CCUS 를 5가 받으면 시멘트·철강 기사가 5와 6에 동시에 걸린다 — 설비·경제성이면 5, 공정·원료면 6 | 3단계 |
| 29 | `mapping_failed` 원칙 강화 — "가장 가까운 것을 고르는 게 목적이 아니다" | 82건에서 실패가 의심스럽게 적었다. 최근접 매칭으로 기울면 subjects 공백 신호가 사라진다 | 3단계 |

---

## 2026-08-21 — subjects 확정 · 3·4단계

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 13 | subjects **13개**로 확정 | 10~15개 권고 범위 안. 82건에 실제로 붙여 입도 확인 | subject |
| 14 | subject 1 확장 — 공시 제도 → 공시·보고 체계 | 의무화 일정·기준제정기구 운영·공시 인프라까지 받는다 | 3단계 |
| 15 | subject 2 확장 — 실사와 인권 규제 → 실사·ESG 데이터 요구 | 구매기업이 협력사에 탄소 데이터를 요구하는 유형이 어디에도 안 붙었다 | 3단계 |
| 16 | 경계 3쌍 추가 — `11↔12`(정부 재정수단은 11) · `1↔12`(기준제정기구 운영은 1) · `2↔4`(요구는 2, 산정은 4) | v2 의 빈칸 | 3단계 |
| 17 | ESG 소프트웨어·플랫폼 기사는 공시 방식·시장 구조 변화가 핵심일 때만 1번. 단순 인수·투자유치는 `mapping_failed` | 플랫폼 인수를 1·12에 넣으면 그 subject 가 다시 캐치올이 된다 | 3단계 |
| 18 | 필터는 `title+snippet` 만 본다 | 본문 32만 자를 필터에서 또 읽으면 비용이 2배 | 3단계 |
| 19 | `subject_name` 도 LLM 이 출력한다. **단 `subject_id` 가 정본**이고 불일치는 코드가 잡아 강등 + 로그 | 이름을 쓰면서 자기 선택을 한 번 더 확인한다. 불일치가 경계 신호다 | 3단계 |
| 20 | `mapping_state` 는 2값. **excluded 분기를 뺀다** | 안 쓸 분기를 남기면 LLM 이 거기로 값을 흘린다 | 3단계 |
| 21 | excluded 판정 자체를 MVP 에서 미룬다 | 82건 중 6~11% 가 노이즈로 흐르지만 4곳 규모면 감당된다 | 3단계 |
| 22 | `mapping_failed` 는 subject NULL. "기타" subject 를 만들지 않는다 | 거기에도 클러스터링이 돌아 억지 이슈가 나온다 | 3단계 |
| 23 | `mapping_failed` 기사는 4단계로 안 보낸다 | 클러스터링이 subject 별로 도니 분석해도 쓸 데가 없다. 나중에 재분석 가능 | 3·4단계 |
| 24 | **`change_type` 3종**(변화·해설·동향). 기존 6종 폐기 | 열도에 필요한 건 "변화가 있었나"지 형식이 아니다. 6종이면 발표/시행/개정 경계에서 흔들리고 그 흔들림이 열도에 노이즈로 들어간다 | 4단계 |
| 25 | `issue_signal` 검증 실패는 **NULL. repair 금지** | 검증기가 값을 만들면 "통과시킨 값"과 "만들어낸 값"이 섞여 품질을 추적할 수 없다 | 4단계 |

---

## 2026-08-19 ~ 20 — 기초

| # | 결정 | 근거 | 단계 |
| --- | --- | --- | --- |
| 1 | DB 는 SQLite 한 파일 | *(#40·41 에서 근거가 교체됨 — 당시 근거는 "n8n 이 쓰고 blogstudio 가 읽는다")* | 1단계 |
| 2 | MVP 는 RSS 전문 4곳만 | 크롤링 없이 먼저 결과가 나오는지 확인 | 2단계 |
| 3 | 크롤링 보류. 11개 소스는 `body_state='pending'` | | 2단계 |
| 4 | 필터와 분석을 한 콜로 안 합친다 | 필터는 싸게 전건, 분석은 비싸게 통과분만 | 3·4단계 |
| 5 | 분석은 **기사당 1콜** | 묶어서 보내면 LLM 이 기사 id 를 드롭한다 | 4단계 |
| 6 | 열도는 코드가 센다 | LLM 에게 "얼마나 핫한가"를 물으면 숫자를 지어낸다 | 6단계 |
| 7 | 하루 소재 수는 개수가 아니라 임계로. **하한 금지, 상한만** | 개수를 고정하면 조용한 날 억지 소재가 나온다 | 7단계 |
| 8 | `published_at` 없는 기사는 `excluded` **로 저장**한다 | 열도가 발행시각에 의존한다. 그렇다고 저장을 포기하면 `url_key` 중복 차단이 안 걸려 매 폴링마다 재등장한다 | 1단계 |
| 9 | 임팩트온 `can_cite=0` | RSS 로 전문이 오지만 독자는 페이월을 본다 | 1단계 |
| 10 | `paywall` 기본값은 `unknown` | `none` 을 기본으로 두면 확인 안 한 걸 무료로 취급하게 된다 | 1단계 |
| 11 | 중복 판정은 `url_key` 하나로. 쿼리를 통째로 털지 않는다 | 국내 CMS 는 `?idxno=` 처럼 쿼리에 기사 id 가 있다 | 1단계 |
| 12 | `rank` 를 저장하지 않는다 | 하나를 제외하면 나머지가 다 밀린다 | 8단계 |

---

## 뒤집힌 결정

| 원래 | 언제 | 무엇으로 | 언제 |
| --- | --- | --- | --- |
| 기사↔소재는 진짜 N:M (관통 소재) | 08-19 | 1:N. N:M 은 보강 기사 정책을 도입할 때 | 08-25 (#57) |
| `migrations/001_init.sql` (user_version 만) | 08-25 | 파일 삭제. 델타는 002 부터 | 08-25 (#62) |
| `as_of = max(collected_at)` | 08-23 | 배치 시작 UTC 시각 | 08-25 (#61) |
| 값의 정본은 `TUNING.md` | 08-25 | `tuning.yaml` (코드가 읽는다) | 08-25 (#65) |
| `requeue` 를 명시적 동작으로 정의 (#59) | 08-25 | MVP 에서 제외. 재분할 경로 설계 시 재검토 | 08-25 (#67) |
| 종료 조건 = `business_relevance` 최솟값 0.2 미만 (#66) | 08-25 | `service_mismatch` 거절분 vs `kept` 상대 비교 | 08-25 (#74) |
| `state_tags` 한글 어휘 | 08-19 | 영문 스네이크 + `topic_reject_tags` 정본 | 08-25 (#72) |
| id↔name 불일치는 `mapping_failed` 로 강등 + 로그 (#19) | 08-21 | 저장하지 않고 422 + 로그 | 08-25 (#93) |
| 필터는 `title+snippet` 만 본다 (#18) | 08-21 | `title` + body 앞 600자(없으면 snippet) | 08-25 (#94) |
| 게이트가 n8n 코드 노드에서 전부 판정 | 08-23 | collector 로. n8n 에는 digest 만 | 08-25 (#92) |
| 기사를 `ArticleIn` 모델로 받는다 (#87 의 전제) | 08-25 | `dict` 로 받고 ingest 안에서 기사 단위 검증 | 08-25 (#88) |
| `articles.published_at` NOT NULL | 08-19 | `CHECK (published_at IS NOT NULL OR state='excluded')` | 08-22 |
| `change_type` 6종 | 08-19 | 3종 | 08-21 (#24) |
| `sources.content_tag` 칼럼 | 08-22 | `config.rss.content_tag` | 08-25 (#53) |
| 관통 가산 +0.1/+0.2 | 08-21 | 제거. `final_score = business_relevance` | 08-23 (#38) |
| `topics.issue_signal` 로 중복 회피 | 08-19 | 배치 소비 마커로 대체. 기록용 필드로 강등 | 08-25 (#44) |
| "n8n 이 쓰는 동안 blogstudio 가 읽는다" | 08-19 | collector 단일 창구 | 08-25 (#40) |
